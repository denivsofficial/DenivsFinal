import React from 'react'

const MoreProperty = () => {
  return (
    <div>
      <h1>More Property Page which contains a list of houses which are having left side scrollable and cards contains the btn which will redirct them to those cards which contains those cards which is the actual card for those propety listing 
      </h1>
      
    </div>
  )
}

export default MoreProperty
